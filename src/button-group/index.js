import NButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
NButtonGroup.install = function(Vue) {
  Vue.component(NButtonGroup.name, NButtonGroup);
};

export default NButtonGroup;
